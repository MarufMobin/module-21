/* function findLargerNumber( numbers ){

    let larger = numbers[ 0 ];

    for( let i = 0; i < numbers.length; i++){

        let element = numbers[ i ];

        if( element > larger ){

            larger = element;
        }
    }
    return larger;
}

const findLargest = [ 25, 58, 45, 49, 25, 91, 37 ];

const resultOfArray = findLargerNumber( findLargest );

console.log( resultOfArray, "That's The Larger Number Of this Array" ); */





// using a function to find out a largest number in array

/* function maxNumberFind( numbers ){

    let maxNumber = numbers[ 0 ];

    for( let i = 0; i < numbers.length; i++ ){

        let element = numbers[ i ];

        if( element > maxNumber ){

            maxNumber = element;
        }
    }
    return maxNumber;
}


const givenNumber = [ 25, 80, 46, 69, 48, 52, 79, 41, 58, 54, 12 ];

const result = maxNumberFind( givenNumber );

console.log(result, "That's Number is Bigger then while array"); */